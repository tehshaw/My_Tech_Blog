<!-- Loop over each object in the projects array -->
{{#each posts as |post| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <a href="/project/{{post.id}}">{{post.title}}</a>
    </h2>
    <p>Posted by: {{post.user.user_name}} on {{post.createdAt}}</p>
    {{!-- <p>
      <span for="img" aria-label="money">💲</span>
      <!-- Pass needed_funding value to the helper function -->
      <span class="dollar-amount"></span>
      needed!
    </p>
    <p>Created by {{post.user.name_name}} on {{format_date post.created_at}}</p> --}}
  </div>
  {{!-- <div class="col-md-7">
    <p>
      {{project.description}}
    </p>
  </div> --}}
</div>
{{/each}}
